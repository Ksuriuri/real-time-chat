[project]
name = "real-time-chat-tts"
version = "0.1.0"
description = "TTS sample scripts for real-time chat agents."
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28",
    "ormsgpack>=1.9",
    "websockets>=15.0",
]

[tool.uv]
package = false
